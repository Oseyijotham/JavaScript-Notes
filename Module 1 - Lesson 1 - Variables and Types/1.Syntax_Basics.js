//Property: - entity.property_name

let num = "JavaScript is awesome".length;
console.log(num);

//Method: - entity.method_name()

let text = "JavaScript is awesome".toUpperCase();
console.log(text);

/*Undefined is a variable that has been declared with the keyword let, 
but has not been initialized with a value.By default, 
it is assigned an initial value of undefined.*/
